[defaults]
roles_path = ansible/roles
# Use a dedicated generated inventory directory; provisioning plays will inject files here.
inventory = ansible/inventory/generated/hetzner.yml

[privilege_escalation]
become=False

[inventory]
enable_plugins = host_list, script, auto, yaml, ini, toml
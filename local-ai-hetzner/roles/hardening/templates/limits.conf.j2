# Ansible managed - System limits configuration
# Conservative limits to prevent system abuse while maintaining functionality

# Limit core dumps
* soft core 0
* hard core 0

# Limit number of processes
* soft nproc 4096
* hard nproc 8192

# Limit number of open files
* soft nofile 1024
* hard nofile 2048

# Root gets higher limits
root soft nproc unlimited
root hard nproc unlimited
root soft nofile 4096
root hard nofile 8192
